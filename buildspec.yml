version: 0.2

phases:
  build:
    commands:
      - echo "Preparing Lambda package"
      - mkdir package
      - cp lambda_function.py package/
      - cd package
      - zip -r ../function.zip .
      - cd ..

artifacts:
  files:
    - function.zip
